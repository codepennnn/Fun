DECLARE @FromDate DATE = '2021-10-01';
DECLARE @ToDate   DATE = '2023-07-01';

SELECT *
FROM YourTable
WHERE ProcMonth BETWEEN 
      CONVERT(VARCHAR(6), @FromDate, 112)
  AND CONVERT(VARCHAR(6), @ToDate, 112);
